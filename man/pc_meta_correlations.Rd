% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_meta_correlations.R
\name{pc_meta_correlations}
\alias{pc_meta_correlations}
\title{Identify associations between principal components and metadata variables}
\usage{
pc_meta_correlations(
  object,
  metadata = NULL,
  reduction = "pca",
  meta.cols = NULL,
  method = c("pearson", "spearman"),
  mode = c("lm", "correlation"),
  adjust = c("BH", "bonferroni", "none"),
  min.cells = 10,
  verbose = TRUE
)
}
\arguments{
\item{object}{A Seurat object, a numeric PCA matrix, or a data frame containing
principal component embeddings.}

\item{metadata}{A data frame of metadata variables. If \code{object} is a Seurat
object, this defaults to the object's metadata. Otherwise this argument is
required.}

\item{reduction}{Name of the PCA reduction to use when \code{object} is a Seurat
object. Default: \code{"pca"}.}

\item{meta.cols}{Character or integer vector of metadata columns to analyse.
Default: all columns.}

\item{method}{Correlation method for numeric metadata or ranking method for
linear-model mode: \code{"pearson"} or \code{"spearman"}.}

\item{mode}{Analysis mode: \code{"lm"} for linear regression or \code{"correlation"}
for simple correlation/ANOVA.}

\item{adjust}{p-value adjustment method passed to \code{p.adjust()}.
Default: \code{"BH"}.}

\item{min.cells}{Minimum non-missing values required for a metadata column.
Default: \code{10}.}

\item{verbose}{If \code{TRUE}, print progress messages.}
}
\value{
A data frame with one row per metadata variable and PC axis pair.
Columns include \code{metadata}, \code{pc}, \code{type}, \code{statistic}, \code{p.value},
\code{adj.p.value}, \code{effect_size}, and \code{direction}.
}
\description{
\code{pc_meta_correlations()} computes associations between PCA axes and metadata
columns. Numeric metadata are tested by correlation, while categorical
metadata are tested by ANOVA and reported with eta-squared effect sizes.
}
